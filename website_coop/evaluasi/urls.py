from django.urls import path
from . import views

app_name = 'evaluasi' # Opsional, tapi praktik yang baik

urlpatterns = [
    # Anda akan menambahkan URL-URL spesifik untuk aplikasi 'akun' di sini nanti.
    # Contoh: path('login/', views.login_view, name='login'),
    # Untuk sekarang, biarkan kosong saja tidak apa-apa.
]